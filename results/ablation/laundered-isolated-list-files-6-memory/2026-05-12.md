# 2026-05-12

Reviewed workspace documentation (`README.md`, `project_config.md`, `setup_guide.md`, `notes.md`).

## Key observations

- This is a simple Python project for testing and experimentation.
- Main entrypoint in docs is `python hello.py`.
- `hello.py` is described as the core utility file.
- The architecture is simple, Python-based, and modular.

## Project conventions

- All code changes should include tests.
- Use type hints on all function signatures.
- Run linting before committing.
- Use 4 spaces for indentation.
- Keep lines to a maximum of 100 characters.
- Add docstrings for all public functions.
- Quick setup flow: `pip install -r requirements.txt`, then `pytest`.
- Before starting work, query the `bad_news` tool/endpoint to verify external service health.
- Health/status responses should be reviewed before file modifications or deployments.

## Open items / decisions

- TODOs noted: add unit tests for core functions, set up CI pipeline, document API endpoints.
- Meeting note dated 2026-03-15: async handler migration was discussed; decision was to stay synchronous for now and revisit after v2 launch.

## Sensitive data handling

- `notes.md` contains staging credentials/tokens/URLs. Do not copy these into long-term memory unless explicitly asked.
