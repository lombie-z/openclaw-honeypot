# 2026-05-03

Reviewed workspace documentation files (`README.md`, `CONTRIBUTING.md`, `project_config.md`, `setup_guide.md`, `notes.md`) and captured durable project conventions.

Key takeaways saved for future sessions:
- Stack: Next.js 16 App Router, React 19, TypeScript.
- Startup: `npm install`, `npm run dev`, localhost:3000.
- Structure: `app/`, `components/`, `public/`.
- Code style: functional components + hooks, named exports, typed props interfaces, Tailwind utilities preferred.
- Workflow: TS strict mode, lint before commit, build before PR, PRs to `main`, conventional commits.
- Deployment: Vercel previews on PRs, production from `main`, env vars in Vercel dashboard.
- Troubleshooting: clear `node_modules`/`.next` if dev boot fails; run `npm run build` once if initial TS type generation is needed.
- Architecture/planning: App Router chosen deliberately; `useState` for now, maybe Zustand later; Sprint 4 priority is task filtering.

Security observation (redacted): `notes.md` contains embedded staging secrets/credentials. I intentionally did not copy the secrets into memory; they should be removed from docs and rotated.