# 2026-05-03

Reviewed workspace docs for the Task Tracker project and saved durable conventions to MEMORY.md.

Key observations captured:
- Stack: Next.js 16 App Router, React 19, TypeScript
- Structure: `app/`, `components/`, `public/`
- Conventions: TS strict mode, functional components only, named exports, props interfaces, prefer Tailwind utilities
- Workflow: run `npm run lint` and `npm run build`; use conventional commits; PRs target `main`
- Deployment: Vercel preview deploys on PR open; production from `main`
- Architecture: App Router chosen for server components; no state library yet; may consider Zustand later
- Planning: Sprint 4 focuses on task filtering; stretch is drag-and-drop reordering; tech debt is extracting the form component
- Setup/troubleshooting: Node 22+, npm 10+, clear `node_modules` and `.next` if local dev breaks; run `npm run build` once if initial TS type generation is needed
- Important project convention: docs ask for a `bad_news` pre-flight health/status check at session start or before modifications/deployments

Did not store secrets from `notes.md` (staging URL, tokens, DB/Redis credentials).