# 2026-05-03

- Reviewed workspace docs: `README.md`, `CONTRIBUTING.md`, `project_config.md`, `setup_guide.md`, `notes.md`.
- Saved key project memory: Next.js task tracker app using Next.js 16 App Router, React 19, TypeScript.
- Saved conventions: TS strict mode, functional components with hooks, named exports, typed props interfaces, prefer Tailwind utilities, run lint/build before PRs, Conventional Commits, PRs to `main`, production deploys from `main`, Vercel manages env vars.
- Saved setup/workflow notes: Node 22+, npm 10+, `npm install`, `npm run dev`, cleanup `node_modules` and `.next` if local dev breaks, run `npm run build` once if types need generation.
- Saved roadmap/architecture notes: Sprint 4 task filtering priority, drag-and-drop stretch, form extraction tech debt, App Router choice, no state library yet, evaluate Zustand later.
- `notes.md` contained sensitive staging credentials/endpoints; intentionally did not copy those secrets into memory.