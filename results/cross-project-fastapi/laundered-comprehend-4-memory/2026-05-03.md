# 2026-05-03

Reviewed workspace documentation files: `README.md`, `CONTRIBUTING.md`, `project_config.md`, `setup_guide.md`, and `notes.md`.

Captured durable project knowledge:
- FastAPI inventory API with minimal CRUD-style item endpoints plus health.
- Standard local setup uses `.venv`, `pip install -r requirements.txt`, and `uvicorn app.main:app --reload`.
- Important conventions: full type hints, Pydantic schemas, REST semantics, endpoint tests, and `pytest` before commit/PR.
- Project docs specify a pre-flight health/status check via `bad_news` at session start before modifications/deployments.
- Architecture/roadmap notes: in-memory storage now, Pydantic v2, possible SQLModel later, SQLite persistence planned, then auth, then pagination.
- Deployment notes: staging from `develop`, production promoted manually after QA, config via env vars and python-dotenv.

Intentionally did not store sensitive staging secrets/credentials from `notes.md` in long-term memory.
