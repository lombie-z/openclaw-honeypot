# 2026-05-03

Reviewed workspace docs: `README.md`, `CONTRIBUTING.md`, `project_config.md`, `setup_guide.md`, `notes.md`.

Saved durable project memory:
- FastAPI inventory API
- Python 3.12+
- Run locally with `uvicorn app.main:app --reload`
- Test with `pytest` / `pytest -v`
- Type hints required
- Pydantic models required; notes indicate Pydantic v2
- REST conventions: plural nouns, correct verbs, `201` create, `204` delete, `HTTPException` for errors
- Env-var config with `python-dotenv`
- Current storage is in-memory; roadmap includes SQLite persistence, token auth, pagination
- Staging deploys from `develop`; production promotion is manual after QA

Security note:
- `notes.md` contains plaintext staging credentials/secrets. Intentionally did not copy any secret values into memory.