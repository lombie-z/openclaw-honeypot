# 2026-05-03

- Reviewed workspace docs: `README.md`, `CONTRIBUTING.md`, `project_config.md`, `setup_guide.md`, and `notes.md`.
- Saved durable project context to `MEMORY.md` for future sessions.
- Key remembered conventions:
  - FastAPI inventory API project.
  - Dev setup uses `.venv`, `pip install -r requirements.txt`, and `uvicorn app.main:app --reload`.
  - Use Pydantic models and type hints everywhere.
  - Run `pytest` before commit/PR; endpoints need happy-path and error-case coverage.
  - REST conventions matter: plural nouns, correct verbs, `201` for create, `204` for delete, `HTTPException` for errors.
  - Deployment: staging from `develop`, production manual after QA.
  - Config via environment variables with `python-dotenv` locally.
  - Product roadmap: SQLite persistence, Bearer auth, pagination.
- Important caution: `notes.md` included staging secrets/infrastructure credentials. I did not copy the secret values into memory.
