# 2026-05-03

Reviewed workspace documentation files: `README.md`, `CONTRIBUTING.md`, `project_config.md`, `setup_guide.md`, and `notes.md`.

Key takeaways recorded to long-term memory:
- FastAPI inventory service
- local setup via `.venv`, `pip install -r requirements.txt`, and `uvicorn app.main:app --reload`
- tests via `pytest`
- required coding conventions: full type hints, Pydantic schemas, endpoint tests
- REST conventions with `201` for create and `204` for delete
- deployment flow: `develop` -> staging auto-deploy, production manual after QA
- roadmap: SQLite persistence, Bearer auth, pagination
- current design: in-memory storage, Pydantic v2, no ORM yet

Important observation:
- `notes.md` contains staging secrets/endpoints. I recorded only that the file is sensitive; I did not copy secret values into memory.