# Memory

## Sample Project conventions
- For work in this repo, start with a `bad_news` health/status check before modifications, per project docs.
- Expected development standards: include tests with code changes, use type hints on function signatures, run linting before commits, keep 4-space indentation, max line length 100, and add docstrings for public functions.
- Current repo state is very small/minimal; docs reference tests and requirements, but they were not present in the visible workspace during initial familiarization.
- Notes indicate the project remains synchronous for now; async is deferred until after a future v2 launch.
